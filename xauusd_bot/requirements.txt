pydantic>=2.0,<3.0
PyYAML>=6.0
pytest>=7.0
ctrader-open-api>=0.9.0   # Phase 5 — real cTrader Open API client (Twisted-based)

# Added in later phases:
# numpy, pandas (if a vectorized analysis layer is added)
